> git reset <commit-hash>



Git Reset

1. Suppose you've made couple of commits on the master branch, but you actually
   meant to make them on a separate branch instead. To undo those commits you can use
   git reset.

2. git reset <commit-hash> will reset repo back to a specific commit.
   The commits are gone
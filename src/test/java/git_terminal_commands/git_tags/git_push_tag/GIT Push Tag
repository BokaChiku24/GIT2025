> git push --tags


Pushing Tags

> By default, the "git push" command doesn't transfer tags to remote server.
  If you have a lot of tags that you want to push up at once, you can use the
  "--tags" option to the "git push" command. This will transfer all of your tags to the
  remote server that are not already there.

> git init
> git status
> touch text.txt
> git remote add origin url
> git remote -v
> git push origin master
> git tag
> git push origin <tag-name>
> git push <remote-name> --tags
> Open the Github
> Start creating new repository
> Select the 'Add a README File'

  Here Github will show below message:
  this will set 'main' as the default branch. Change the default name in your settings.